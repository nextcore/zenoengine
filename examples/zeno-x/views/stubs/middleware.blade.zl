// Middleware: {{ $name }}
// Generated by Zeno Artisan at {{ $date }}

middleware.define: '{{ $name }}' {
    do: {
        // Middleware Logic
        // if: ... { return: response.redirect(...) }
        
        // Continue to next handler
        return: middleware.next
    }
}
