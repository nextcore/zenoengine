name: php-native
version: 1.3.0
type: sidecar
binary: ./php_bridge.exe  # After compiling with: zig build-exe main.zig -O ReleaseSafe

sidecar:
  protocol: json-rpc
  auto_start: true
  keep_alive: true

permissions:
  filesystem:
    - ./app
    - ./vendor
  network:
    - "*"
