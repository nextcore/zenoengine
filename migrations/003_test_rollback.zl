up {
    db.create_table: 'tasks' {
        db.id: 'id'
        db.string: 'title'
        db.integer: 'user_id'
        db.foreign: 'user_id' {
            references: 'test_users'
            on: 'id'
        }
    }
}

down {
    db.drop_table: 'tasks'
}
