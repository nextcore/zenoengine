# ==========================================
# 1. SERVER CONFIGURATION
# ==========================================
# Application port
APP_PORT=:3000

# Application Mode
# development = Show detailed errors in browser (Best for coding)
# production  = Hide errors, show generic messages (Best for live)
APP_ENV=development
# Live Reload Configuration (in development)
# LIVERELOAD_ENABLED=true

# Rate Limiting
# Maximum number of requests per time window
RATE_LIMIT_REQUESTS=100
# Time window in seconds (60 = 1 minute)
RATE_LIMIT_WINDOW=60

# Request Timeout (Context-Aware Executor)
# Maximum execution time for HTTP requests to prevent infinite loops
# Format: duration string (e.g., "30s", "1m", "2m30s")
ZENO_REQUEST_TIMEOUT=30s


# ==========================================
# 2. DATABASE (MySQL / SQLite / Postgres)
# ==========================================
# Driver Options: mysql, sqlite, postgres, sqlserver
DB_DRIVER=sqlite

# Format: IP:PORT
# DB_HOST=127.0.0.1:3306
# DB_USER=root
# DB_PASS=
DB_NAME=./zeno.db
# Database Connection Pool Configuration
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5

# ==========================================
# 3. SECURITY (JWT & CSRF)
# ==========================================
# Secret key for token signing. 
# Run 'zeno key:generate' to set this automatically in your local .env
JWT_SECRET=
CSRF_TOKEN=your_csrf_token_here
# Comma separated list of trusted origins for CSRF protection
TRUSTED_ORIGINS=localhost,127.0.0.1


# ==========================================
# 4. EMAIL SERVICE (SMTP)
# ==========================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# ==========================================
# 5. ADDITIONAL DATABASES (Multi-Database Support)
# ==========================================
# The engine automatically detects additional DBs if variables prefixed with DB_[NAME]_ 
# are present with suffixes _DRIVER, _HOST, _NAME, _USER, or _PASS.
# These databases can be called in ZenoLang using db: name

# Example 1: MySQL (Warehouse)
# DB_GUDANG_DRIVER=mysql
# DB_GUDANG_HOST=127.0.0.1:3306
# DB_GUDANG_USER=root
# DB_GUDANG_PASS=
# DB_GUDANG_NAME=db_warehouse

# Example 2: PostgreSQL (Analytics)
# DB_ANALYTICS_DRIVER=postgres
# DB_ANALYTICS_HOST=127.0.0.1:5432
# DB_ANALYTICS_USER=postgres
# DB_ANALYTICS_PASS=password
# DB_ANALYTICS_NAME=analytics_db

# Example 3: SQLite (CMS)
# DB_ZENOCMS_DRIVER=sqlite
# DB_ZENOCMS_NAME=./zenocms.db

# ==========================================
# 6. TUTORIAL MAX DEMO (SQLite)
# ==========================================
# Used by src/tutorial/max/
DB_TUTORIAL_DRIVER=sqlite
DB_TUTORIAL_NAME=./tutorial_max.db

# ==========================================
# 7. REDIS (Distributed Cache)
# ==========================================
REDIS_HOST=localhost:6379
REDIS_PASS=
REDIS_DB=0

# ==========================================
# 8. WORKER & SECURITY
# ==========================================
WORKER_ENABLED=false

# Content Security Policy (CSP)
# Set to 'true' or 'enable' to activate security headers. Disabled by default.
CSP_ENABLED=false
# CSP Allowed Domains (space separated)
CSP_ALLOWED_DOMAINS="https://code.jquery.com https://unpkg.com"

# ==========================================
# 9. MULTI-TENANT POS TUTORIAL (SQLite)
# ==========================================
# System Database (Tenants Registry)
DB_SYSTEM_DRIVER=sqlite
DB_SYSTEM_NAME=./data/pos_system.db

# Tenant ABC (Demo Tenant)
DB_TENANT_ABC_DRIVER=sqlite
DB_TENANT_ABC_NAME=./data/pos_tenant_abc.db

# Tenant XYZ (Demo Tenant)
DB_TENANT_XYZ_DRIVER=sqlite
DB_TENANT_XYZ_NAME=./data/pos_tenant_xyz.db
# ==========================================
# 10. GOOGLE SHEETS API (GoCMS Tutorial)
# ==========================================
# The Spreadsheet ID (found in the URL)
# GSHEET_ID=1abc123...
# Path to Service Account JSON file or raw JSON content
# GSHEET_CREDS=./google-creds.json
