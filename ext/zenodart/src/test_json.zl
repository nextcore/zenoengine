$data: {
    name: "Zeno"
    type: "Engine"
}

json.stringify: $data {
    as: "jsonString"
}

log: "Stringified: " + $jsonString

json.parse: $jsonString {
    as: "parsedData"
}

log: "Parsed Name: " + $parsedData.name
