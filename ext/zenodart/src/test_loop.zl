$list: [1, 2, 3]
foreach: $list {
    log: $it
}

$users: [alice, bob]
foreach: $users {
    log: "User: " + $it
}

$map: {
    foo: "bar"
    baz: "qux"
}
foreach: $map {
    log: $key
    log: $value
}
