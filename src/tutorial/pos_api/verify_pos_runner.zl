// Runner for POS API Verification
log: "Starting POS API Verification Server on port 3333..."

// Include the main module definitions
include: src/tutorial/pos_api/main.zl

// Start Server on custom port to avoid conflicts
http.server: {
    port: 3333
}
